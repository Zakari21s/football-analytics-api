"""
Players API – read-only dataset resource.
Endpoints: GET /players, GET /players/{player_id} (to be implemented).
"""

from fastapi import APIRouter

router = APIRouter()

# Endpoints will be added here (list, get by id, performances, transfers)
